#include<stdio.h>
//tniromin
//The calculation is done before taking the relevant input or inputs
//solution :Take the input first

int main(){
int sum=100,num;

sum=sum+num;
scanf("%d",&num);


return 0;
}
//I know this is a lame logical error but everything must have a start.
